import 'package:flutter/material.dart';

class AppPaddings{
  AppPaddings._();

  static const a10 = EdgeInsets.all(10);
  static const a20 = EdgeInsets.all(20);
  static const a40 = EdgeInsets.all(40);
  static const a12 = EdgeInsets.all(12);
  static const a5 = EdgeInsets.all(5);
  static const lr5 = EdgeInsets.symmetric(horizontal: 5);
  static const lr10 = EdgeInsets.symmetric(horizontal: 10);
  static const lr18 = EdgeInsets.symmetric(horizontal: 18);
  static const r5 = EdgeInsets.only(right: 5);

}